# วิธีออกเวอร์ชันใหม่ (ให้ปุ่ม "อัปเดตแอป" ในมือถือเจอ)

แอปจะเช็ก **GitHub Releases** ของ repo `innosenze88/accounting` ถ้าเจอเวอร์ชันที่ใหม่กว่า จะดาวน์โหลด APK แล้วติดตั้งทับของเดิม ข้อมูลในเครื่องจะอยู่ครบ

## ทุกครั้งที่จะออกเวอร์ชันใหม่

1. แก้ `app/build.gradle.kts`
   - `versionCode` = เลขเดิม **+1** (ต้องมากขึ้นทุกครั้ง)
   - `versionName` = เวอร์ชันใหม่ เช่น `"1.2.0"`
2. Android Studio → **Build → Build App Bundle(s) / APK(s) → Build APK(s)**
   - ไฟล์อยู่ที่ `app/build/outputs/apk/debug/app-debug.apk`
3. Commit & Push โค้ดขึ้น GitHub
4. เข้า GitHub → repo → **Releases → Draft a new release**
   - Tag: `v1.2.0` (ต้องตรงกับ versionName และมี `v` นำหน้า)
   - ช่องรายละเอียด: เขียนว่ามีอะไรใหม่ (จะแสดงในแอป)
   - ลากไฟล์ `.apk` ใส่ แล้วกด **Publish release**
5. ในมือถือ: แท็บ **ตั้งค่า → อัปเดตแอป → ตรวจสอบอัปเดต**

## สำคัญมาก: ข้อมูลไม่หายเพราะ 3 อย่างนี้

| เงื่อนไข | ถ้าไม่ตรง |
|---|---|
| ชื่อแพ็กเกจเดิม (`applicationId`) | ติดตั้งเป็นแอปใหม่อีกตัว |
| **เซ็นด้วย keystore เดิม** (`debug.keystore` ในโฟลเดอร์โปรเจกต์) | Android ไม่ยอมติดตั้งทับ |
| `versionCode` มากกว่าเดิม | Android ไม่ยอมติดตั้งทับ |

แอปจะตรวจทั้ง 3 ข้อให้ก่อนติดตั้ง ถ้าไม่ผ่านจะบอกเหตุผล และจะไม่ติดตั้ง

- **สำรองไฟล์ `debug.keystore` ไว้ที่อื่นด้วย** (ไฟล์นี้ไม่ได้ขึ้น GitHub) ถ้าไฟล์นี้หาย จะอัปเดตทับไม่ได้อีก
- **ห้ามลบแอปแล้วลงใหม่** เพราะข้อมูลในเครื่องจะหายทั้งหมด
- ถ้าเปลี่ยนโครงสร้างฐานข้อมูล ต้องเพิ่ม Migration ใน `AppDatabase.kt` ทุกครั้ง (ห้ามใช้ destructive migration)
